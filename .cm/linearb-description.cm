# -*- mode: yaml -*-

manifest:
  version: 1.0

automations:
  describe_changes_by_linearb_ai:
    if:
      - {{ pr.title | includes(term="describe") }}
    run:
      - action: describe-changes@v1
        args:
          concat_mode: append

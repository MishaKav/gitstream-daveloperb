# -*- mode: yaml -*-

manifest:
  version: 1.0

automations:
  assign_knowledge_sharing:
    if:
       - {{ not pr.draft }}
    run:
      - action: add-reviewers@v1
        args:
          reviewers: {{ repo | codeExperts(gt=90) | random }}
      - action: add-comment@v1
        args:
          comment: |
            gitStream has assigned a reviewer to increase knowledge sharing on this PR.
